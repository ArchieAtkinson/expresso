#include <iostream>

#include "lexer.hpp"



int main() {
    // std::string input;
    // while(std::getline(std::cin, input)) { // quit the program with ctrl-d
    //     auto lex = Lexer(input);
    // }
    auto lex = Lexer("1+-*/()");
}